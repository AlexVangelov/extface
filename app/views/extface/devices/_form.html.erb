<%= form_for(@device) do |f| %>
  <% if @device.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@device.errors.count, "error") %> prohibited this device from being saved:</h2>

      <ul>
      <% @device.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <h5>Extfaceable: <%= @device.inspect %></h5>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :driver %><br>
    <%= f.select :driveable_type, options_for_drivers, { include_blank: true }, class: 'form-control' %>
  </div>
  <div class="actions">
    <%= f.submit nil, name: :submit, value: :submit %>
  </div>
<% end %>
